# 사업비서 — 공고 수집 백엔드

기존 index.html을 유지하면서 Node.js 24 + SQLite 백엔드를 연결했습니다. 외부 npm 패키지는 필요 없습니다.

## 실행

1. 이 폴더에서 `npm run setup` — 기존 .env는 덮어쓰지 않습니다.
2. `.env`의 `BIZINFO_API_KEY=`에 기업마당에서 발급받은 인증키를 입력합니다. 키를 HTML이나 채팅에 넣지 마세요.
3. `npm start`
4. http://127.0.0.1:4180/ 서비스 / http://127.0.0.1:4180/admin 관리자

관리자 화면의 인증 값은 `.env`의 `ADMIN_TOKEN`입니다. setup이 무작위로 생성합니다. 키를 변경했으면 서버를 재시작합니다.

## 구현한 범위

- 기업마당 공식 JSON API 페이지 수집, 요청 제한 시간, 429/서버 오류 재시도
- SQLite 영구 보관: 공고 정규화, 원본 JSON 버전, 최초/최근 관측일, 수정 버전, 실행 이력, 잘못된 항목 격리
- 전체 응답을 받은 뒤 트랜잭션으로 반영. 중간 실패는 이전 공고를 유지합니다.
- 중복 페이지·총건수 변경·기존 DB가 있는데 빈 응답인 경우 실패 처리. 일부 잘못된 항목/중복은 partial 표시.
- 목록에서 사라졌다는 이유만으로 공고를 마감 처리하지 않습니다.
- 서버 실행 중 기본 24시간 간격 수집. 재시작 시 예정 시간이 지났으면 수집, 실패 시 기본 60분 후 재시도. 다음 예정 시각은 DB에 보관합니다.
- 관리자 토큰으로 수동 수집/실행 이력 조회. .env와 DB는 웹으로 공개하지 않습니다.
- `/api/programs`는 최대 5,000건과 전체 수/잘림 여부를 반환합니다. 프런트도 이를 표시합니다.

## 인증키 없을 때

DB는 실제 수집 공고 0건으로 시작합니다. 가짜 공고를 DB에 넣지 않습니다. 화면은 ‘API 키 설정 대기’를 표시하고 기존 수동 확인 공고를 사용합니다. 테스트의 2,000건은 테스트용 메모리 DB에만 들어갑니다.

`npm run sync` 수동 수집 / `npm run status` 상태 / `npm run import -- "공식API응답.json"` 파일 가져오기 / `npm test` 백엔드 검사 / `node verify.cjs` 기존 화면 로직 검사.

파일 가져오기는 실시간 API 수집과 구분하여 저장합니다. 자동 수집을 끄려면 `SYNC_ENABLED=false`. 데이터는 `data/business-assistant.sqlite`에 저장합니다. 백업할 때 서버를 종료한 뒤 data 폴더 전체를 복사하세요. .env에는 비밀키가 있으므로 공개 저장소에 올리지 마세요.

## 다음 단계와 한계

이번 구현은 요청한 1단계 ‘실제 데이터 자동수집’입니다. API 키 발급 및 실제 인증된 응답 검증은 별도로 필요합니다. API가 태그/요약으로 제공한 정보는 자격판정 근거로 확정하지 않습니다. 공고의 analysisStatus는 pending, eligibility는 null입니다. 기존 수동 공고 외 API 공고에는 자격 확률이나 충족 조건을 꾸며 넣지 않습니다.

다음은 첨부파일 수집·본문 추출 → 근거 문장과 함께 조건 구조화 → 운영자 검토 → 프로필 매칭입니다. 현재 첨부파일은 링크만 보관하며 AI 호출, 자동 이메일/카카오 발송, 회원가입, 실제 결제는 구현하지 않았습니다. 기존 접수 화면은 별도 접수 서버가 없으면 다운로드 미리보기로 동작합니다.

이 서버는 로컬 MVP용입니다. 공개 서비스 전에는 HTTPS, 사용자 인증/권한, 운영 모니터링, 백업과 개인정보 처리 체계를 갖춰야 합니다. 서버를 종료하면 수집도 중단됩니다. Windows 작업 스케줄러/클라우드 상시 실행은 이번에 설치하지 않았습니다.

## 공식 데이터 출처

- 기업마당 API 명세·인증키 신청: https://www.bizinfo.go.kr/apiDetail.do?id=bizinfoApi
- 실제 호출: https://www.bizinfo.go.kr/uss/rss/bizinfoApi.do (crtfcKey, dataType=json, pageUnit, pageIndex, searchCnt)
- 기업마당 콘텐츠 이용정책: https://www.bizinfo.go.kr/biz/bizd/selectBIZD120View.do — API 인증키 발급과 콘텐츠의 수익 이용 허락을 동일하게 취급하지 않습니다.
- 중소벤처24 후보 API: https://www.data.go.kr/data/15113191/openapi.do — 상세 호출 규격을 확인한 후 별도 수집기를 추가합니다. 이번 버전은 기업마당만 구현했습니다.

최종 자격은 공식 공고와 기관 확인이 필요합니다. 융자와 보조금은 구분해야 하며 분석되지 않은 지원방식은 unknown 상태로 보관합니다.
